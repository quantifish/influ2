% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-brms.R
\name{influ.brmsfit}
\alias{influ.brmsfit}
\title{Influence diagnostics for brms models}
\usage{
\method{influ}{brmsfit}(
  model,
  focus,
  data = NULL,
  weights = NULL,
  uncertainty = "auto",
  retain = "summary",
  probs = c(0.025, 0.975),
  ndraws = NULL,
  seed = NULL,
  draws_path = NULL,
  keep_model = FALSE,
  ...
)
}
\arguments{
\item{model}{A fitted object from [brms::brm()].}

\item{focus}{Name of the focus variable, usually year.}

\item{data}{Optional model data. The model frame is used by default.}

\item{weights}{Optional numeric observation weights or the name of a weight
column in `data`.}

\item{uncertainty}{Either `"auto"`, `"posterior"`, or `"none"`. The
`"none"` mode reduces the posterior to coefficient means before influence
calculations.}

\item{retain}{One of `"summary"`, `"derived_draws"`, or `"disk"`.}

\item{probs}{Lower and upper interval probabilities.}

\item{ndraws}{Optional maximum number of posterior draws. `NULL` uses every
available draw.}

\item{seed}{Optional simulation seed.}

\item{draws_path}{Output path used when `retain = "disk"`.}

\item{keep_model}{Retain the fitted model inside the diagnostic object.}

\item{...}{Reserved for future backend options.}
}
\value{
An [influ_diag] object.
}
\description{
Population-level posterior coefficients are projected directly into the
compact focus-by-term contrasts. The method therefore never constructs an
observation-by-draw-by-term array. Joint posterior dependence is preserved
while calculating the requested diagnostics.
}
