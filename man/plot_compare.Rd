% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/additional-diagnostics.R
\name{plot_compare}
\alias{plot_compare}
\title{Compare standardised CPUE indices}
\usage{
plot_compare(
  fits,
  labels = NULL,
  year = NULL,
  probs = c(0.25, 0.75),
  show_probs = TRUE,
  rescale = "raw",
  rescale_series = NULL,
  ...
)
}
\arguments{
\item{fits}{A fitted model, an [influ_diag], or a list of either.}

\item{labels}{Optional unique, non-empty model labels. Repeated automatically
generated labels are disambiguated with numeric suffixes.}

\item{year}{Optional focus-variable name. It is inferred when omitted.}

\item{probs}{Interval probabilities used when diagnostics must be calculated.}

\item{show_probs}{Show uncertainty ribbons.}

\item{rescale}{`"raw"`, or a positive numeric geometric mean.}

\item{rescale_series}{Optional series number supplying the common scale over
overlapping focus levels.}

\item{...}{Arguments passed to [influ()] when `fits` contains models.}
}
\value{
A [ggplot2::ggplot()] object.
}
\description{
Compares indices from any fitted models supported by [influ()] or from
pre-computed [influ_diag] objects. Models are reduced to the same index
schema before plotting.
}
\details{
Compared indices must be on the same scale: response ratios,
  response differences, or link-scale contrasts. Ratio plots start at zero;
  difference and link-scale plots retain negative values. Inputs should
  describe comparable responses and focus effects; matching scales alone
  does not establish that the fitted models answer the same question.
}
