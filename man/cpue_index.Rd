% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue-index.R
\name{cpue_index}
\alias{cpue_index}
\alias{as.data.frame.influ_index}
\alias{print.influ_index}
\title{Calculate an assessment-ready CPUE index}
\usage{
cpue_index(
  model,
  year = NULL,
  method = c("standardised", "standardized", "year_effect"),
  reference_data = NULL,
  reference_weights = NULL,
  uncertainty = c("auto", "none"),
  probs = c(0.025, 0.975),
  rescale = "raw",
  ndraws = 1000L,
  batch_size = 250L,
  draw_batch_size = 100L,
  retain = c("summary", "draws"),
  units = NULL,
  ...
)

\method{as.data.frame}{influ_index}(x, row.names = NULL, optional = FALSE, ...)

\method{print}{influ_index}(x, ...)
}
\arguments{
\item{model}{A fitted GLM, \code{mgcv} GAM, \code{glmmTMB}, or complete \code{brmsfit} for
response standardisation. For \code{method = "year_effect"}, any model supported
by \code{\link[=influ]{influ()}} or an existing \code{influ_diag} can be supplied.}

\item{year}{Name of the year variable. Defaults to the first formula predictor.}

\item{method}{\code{"standardised"} (default), its alias \code{"standardized"}, or
\code{"year_effect"}. The latter is a contrast, not an expected-response index.}

\item{reference_data}{A non-empty data frame defining a common covariate
profile or population, \strong{without} the year column. Each row is predicted
in every observed year. Supply all required predictors and any exposure
variables explicitly; use one unit of exposure for a per-unit index.}

\item{reference_weights}{Non-negative weights, one per reference row.
Defaults to equal weights. These are standardisation weights, not areas.}

\item{uncertainty}{\code{"auto"} for propagated uncertainty, or \code{"none"} for
a point-estimate preview with missing uncertainty columns.}

\item{probs}{Two increasing interval probabilities.}

\item{rescale}{\code{"raw"} (default), or a positive target geometric mean across
all returned years. A common normalisation is applied to each posterior
draw; frequentist uncertainty includes the normalising denominator.}

\item{ndraws}{Maximum number of existing posterior draws to use for brms.
Deterministically spaced draw identities are shared across years and batches.}

\item{batch_size}{Maximum reference rows predicted together.}

\item{draw_batch_size}{Maximum posterior draws predicted together.}

\item{retain}{\code{"summary"} (default) or \code{"draws"}. Draw retention is available
for standardised brms indices and stores only a draw-by-year matrix.}

\item{units}{Optional response units, such as \code{"lobsters per pot"}.}

\item{...}{Arguments passed to \code{\link[=influ]{influ()}} only for \code{method = "year_effect"}.}

\item{x}{An `influ_index` object.}

\item{row.names, optional}{Passed to the data-frame method.}
}
\value{
An S3 \code{influ_index} object containing \code{table}, \code{metadata}, and optional
\code{draws}. \code{as.data.frame()} returns the assessment table with \code{Year}, \code{Mean},
\code{Median}, \code{SD}, \code{CV}, \code{Qlower}, \code{Qupper}, \code{Method}, \code{Distribution}, and \code{Link}.
}
\description{
Calculate expected-response indices over the same explicit reference
population in every year, or extract the existing year-effect contrasts.
No model is fitted or updated by this function.
}
\details{
For standardisation, predictions are averaged on the \strong{response}
scale, not averaged on the link scale and then back-transformed. Supplied
reference weights are treated as known. Hurdle and zero-inflated fits use
the backend's combined expected response, including its zero component.
Binomial GLM/glmmTMB predictions are probabilities; brms binomial expected
predictions are counts at the supplied number of trials. Supply one trial
for comparable per-trial indices. Log-transformed responses are rejected:
fit an explicit lognormal model instead of silently exponentiating a fit.

GLMs and GAMs use the delta method and their joint coefficient covariance.
GAM smooths, including random-effect smooths, are evaluated as fitted; the
smoothing-parameter correction is used when available. glmmTMB uses the
joint fitted-parameter covariance and numerical derivatives of native
response predictions, with conditional random effects set to zero. REML
and zero-component random effects are not yet supported for glmmTMB here.
brms uses \code{\link[brms:posterior_epred]{brms::posterior_epred()}} with group-level effects set to zero.
Setting a random effect to zero is \strong{not} integration over its population
distribution. brms smooths and other population-level terms remain present.

\code{Mean} is the fitted expected-response estimate for frequentist models,
and the posterior mean of the expected-response index for brms. \code{Median}
is a posterior median and is \code{NA} for frequentist estimates, rather than
labelling an MLE as a posterior median. \code{SD} is the standard error or
posterior standard deviation of the index, not observation dispersion.
\code{CV} is \code{SD / Mean} when the mean is positive. Positive frequentist indices
have delta-method log-scale intervals; other indices have normal intervals.
These intervals are pointwise, not simultaneous. Preview mode retains the
brms posterior mean, but omits uncertainty summaries; it does not replace
joint predictions with predictions at posterior-mean coefficients.

Working storage is bounded by reference and draw batches plus a compact
annual covariance or draw matrix. Native prediction code can allocate
additional memory. The result does not retain the model or reference data.
Spatial response standardisation and area integration are separate future
adapters; \code{sdmTMB} and \code{tinyVAST} currently support \code{"year_effect"} here.
Year-effect results preserve the original diagnostic estimand and cannot
be rescaled by this function. These indices are not biomass estimates.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
  data(lobsters_per_pot)
  fit <- glmmTMB::glmmTMB(lobsters ~ year + depth + (1 | month),
    family = glmmTMB::nbinom2(), data = lobsters_per_pot)
  index <- cpue_index(fit, year = "year",
    reference_data = data.frame(depth = 40), units = "lobsters per pot")
  head(as.data.frame(index))
  plot_index(index)
}
}
\seealso{
\code{\link[=plot_index]{plot_index()}}, \code{\link[=plot_compare]{plot_compare()}}, \code{\link[=geo_mean]{geo_mean()}}, \code{\link[=influ_indices]{influ_indices()}}
}
