% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-output.R
\name{index_table}
\alias{index_table}
\title{Format a calculated index for assessment reporting}
\usage{
index_table(
  x,
  format = c("summary", "lognormal"),
  include_median = c("auto", "always", "never")
)
}
\arguments{
\item{x}{An \code{influ_index} from \code{\link[=cpue_index]{cpue_index()}} or \code{\link[=integrate_index]{integrate_index()}}.}

\item{format}{\code{"summary"} for the existing index summaries, or \code{"lognormal"}
to append a moment-matched lognormal approximation to marginal index
uncertainty. This is not the fitted response distribution.}

\item{include_median}{\code{"auto"} omits \code{Median} only when every value is
missing; \code{"always"} retains it, and \code{"never"} omits it. An available brms
posterior median is never replaced by a lognormal approximation.}
}
\value{
A data frame in the stored year order. The input is unchanged.
}
\description{
Return a reporting table without refitting or repeating predictions. The
full, stable table is still available through \code{\link[=as.data.frame.influ_index]{as.data.frame.influ_index()}}.
}
\details{
\code{Mean}, \code{SD}, and \code{CV} refer to the expected-response index and its
uncertainty, not variability among individual observations. With
\code{format = "lognormal"}, \code{SDlog = sqrt(log(1 + CV^2))},
\code{Meanlog = log(Mean) - SDlog^2 / 2}, and
\code{LognormalMedian = exp(Meanlog)}. These additional columns describe a
univariate moment-matched approximation, even when the CPUE model itself
is not lognormal. They require positive means and finite non-negative
index SDs. Preview and year-effect results cannot provide this output.

\code{Meanlog} is not \code{log(Mean)}, and \code{SDlog} is not automatically the square
root of the diagonal of \code{\link[=index_vcov]{index_vcov()}} on the log scale. The latter comes
from the actual joint delta method or log-index draws, not marginal
lognormal moment matching. Use the joint matrix for a correlated-index
assessment; do not combine its correlations with these approximate SDs
without explicitly choosing a different uncertainty model.
}
\examples{
data(lobsters_per_pot)
fit <- glm(lobsters ~ year + depth, poisson(), data = lobsters_per_pot)
index <- cpue_index(fit, reference_data = data.frame(depth = 40))
head(index_table(index))
head(index_table(index, format = "lognormal"))
}
\seealso{
\code{\link[=index_vcov]{index_vcov()}}, \code{\link[=plot_index]{plot_index()}}, \code{\link[=cpue_index]{cpue_index()}}
}
